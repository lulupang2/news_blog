---
id: "48184322"
title: "Hyperpolyglot Lisp: Common Lisp, Racket, Clojure, Emacs Lisp"
translatedTitle: "리습语言比較: 커먼 리습, 랙켓, 클로저, 엘리시프 리습"
url: "https://hyperpolyglot.org/lisp"
author: "veqq"
score: 119
commentsCount: 24
date: "2026-05-18T19:27:25.000Z"
summary: "요약하자면 리습 언어들의 문법과 실행 방식 비교임 ㅋㅋㅋ"
image: ""
---

> **🔗 원문 보러가기:** [Hyperpolyglot Lisp: Common Lisp, Racket, Clojure, Emacs Lisp](https://hyperpolyglot.org/lisp)


### 리습 언어 비교
리습 언어들은 各自의 특징과 문법이 있습니다. 아래는 커먼 리습, 랙켓, 클로저, 엘리시프 리습의 비교입니다.

#### 변수와 식
*   변수 선언: 커먼 리습은 `let`과 `let*`을 사용하고, 랙켓은 `let`과 `let*`을 사용하며, 클로저는 `let`을 사용합니다.
*   식: 커먼 리습은 `(+ 1 2)`와 같이 사용하며, 랙켓과 클로저도 podob하게 사용합니다.

#### 데이터 타입
*   정수: 커먼 리습은 `integer` 타입을 사용하며, 랙켓은 `integer?` 함수를 사용하고, 클로저는 `Integer` 타입을 사용합니다.
*   실수: 커먼 리습은 `float` 타입을 사용하며, 랙켓은 `real?` 함수를 사용하고, 클로저는 `Float` 타입을 사용합니다.

#### 제어 구조
*   조건문: 커먼 리습은 `if` 문을 사용하며, 랙켓과 클로저도 podob하게 사용합니다.
*   반복문: 커먼 리습은 `loop` 문을 사용하며, 랙켓은 `for` 문을 사용하고, 클로저는 `loop` 함수를 사용합니다.

#### 함수
*   함수 정의: 커먼 리습은 `defun`을 사용하며, 랙켓은 `define`을 사용하고, 클로저는 `defn`을 사용합니다.
*   함수 호출: 커먼 리습은 `(funcall foo 1 2)`와 같이 사용하며, 랙켓과 클로저도 podob하게 사용합니다.

#### 예외 처리
*   커먼 리습은 `handler-case`를 사용하며, 랙켓은 `try`-`catch`를 사용하고, 클로저는 `try`-`catch`를 사용합니다.

### 마무리
리습 언어들은 各自의 특징과 문법이 있습니다. 이 비교는 리습 언어들의 주요특징과 문법을 빠르게 비교하는 데 도움이 될 것입니다. ㅋㅋㅋ
