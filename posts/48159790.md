---
id: "48159790"
title: "My Favorite Bugs: Invalid Surrogate Pairs"
translatedTitle: "🚀 SURROGATE PAIR 핵폭탄 버그! 🔴 디벨로퍼가 겪은 지옥의 디버깅"
url: "https://george.mand.is/2026/05/my-favorite-bugs-invalid-surrogate-pairs/"
author: "meysamazad"
score: 27
commentsCount: 6
date: "2026-05-16T12:49:07.000Z"
summary: " SURROGATE PAIR 버그 때문에 편집기가 멈추는 거지😱"
image: "https://georgemandis.s3-us-west-1.amazonaws.com/preview.png"
---

> **🔗 원문 보러가기:** [My Favorite Bugs: Invalid Surrogate Pairs](https://george.mand.is/2026/05/my-favorite-bugs-invalid-surrogate-pairs/)


### SURROGATE PAIR 핵폭탄 버그!
 SURROGATE PAIR 버그 때문에 편집기가 멈추는 거지😱 

 SURROGATE PAIR란อะไร냐하면, 자바스크립트에서 유니코드 문자를 처리할 때 사용하는 방법이야. 자바스크립트는 유니코드 문자를 16비트로 저장하고, 16비트로는 표현할 수 없는 문자들은 두 개의 16비트 코드 유닛을 사용해서 저장한다고. 

이 버그는 크래쉬도 안 있고, 에러도 안 나고, 그냥 편집기 멈추는 거지.😱 그리고 이 버그는 특정한 에모지들을 입력했을 때만 발생한다니 더더욱 어이없다. 

디벨로퍼가 이 버그를 찾기까지 얼마나 고생했는지 가득가득 담긴 이야기임. 
### 디벨로퍼의 고통
 디벨로퍼는 이 버그를 찾기 위해 얼마나 많은 시간과 노력을 않았을까?😂  이 버그는 에디터의 심장부인 CRDT 라이브러리에서 발생했고, 디벨로퍼는 이 버그를 찾아내기 위해 많은 시도를 했다. 

 디벨로퍼는SURROGATE PAIR를 분리하는 코드를 작성했고, 이 때문에 버그가 발생했다는 것을発现했다. 그리고 디벨로퍼는 이 버그를 해결하기 위해 많은 노력을 했지만, 쉽지 않았다. 

### SURROGATE PAIR란?
 SURROGATE PAIR는 자바스크립트에서 유니코드 문자를 처리할 때 사용하는 방법이야. 자바스크립트는 유니코드 문자를 16비트로 저장하고, 16비트로는 표현할 수 없는 문자들은 두 개의 16비트 코드 유닛을 사용해서 저장한다고. 

 예를 들어, 🤠라는 에모지는 한개의 유니코드 문자지만, 자바스크립트에서는 두 개의 16비트 코드 유닛으로 저장된다. 

### 개선 방안
 디벨로퍼는 이 버그를 해결하기 위해 여러 가지 방법을 시도했다. 먼저,SURROGATE PAIR를 분리하는 코드를 수정해서 버그를 해결했다. 그리고 에디터의 핵심부인 CRDT 라이브러리를 수정해서 버그를 해결했다. 

 그리고 디벨로퍼는 SURROGATE PAIR를 분리하는 코드를 작성할 때, 자바스크립트의 `Intl.Segmenter`를 사용해서 버그를 해결했다. 

### 결론
 이 버그는 디벨로퍼가 겪은 지옥의 디버깅이었다. 디벨로퍼는 이 버그를 찾기 위해 많은 시간과 노력을 했고, 결국에는 버그를 해결했다. 

 이 버그는 SURROGATE PAIR에 대한 이해가 부족해서 발생했다. SURROGATE PAIR는 자바스크립트에서 유니코드 문자를 처리할 때 사용하는 방법이고, 디벨로퍼는 이 방법을 수정해서 버그를 해결했다. 

 이 이야기는 디벨로퍼가 겪은 지옥의 디버깅이었다. 디벨로퍼는 이 버그를 찾기 위해 많은 시간과 노력을 했고, 결국에는 버그를 해결했다. 

 디벨로퍼는 이 버그를 해결하기 위해SURROGATE PAIR를 분리하는 코드를 수정하고, 에디터의 핵심부인 CRDT 라이브러리를 수정했으며, 자바스크립트의 `Intl.Segmenter`를 사용해서 버그를 해결했다. 

 이 이야기는 디벨로퍼가 겪은 지옥의 디버깅이었다. 디벨로퍼는 이 버그를 찾기 위해 많은 시간과 노력을 했고, 결국에는 버그를 해결했다. 

